{
  "kind": "build_request",
  "title": "Fix food items not displaying on frontend",
  "projectName": "Food Ordering App",
  "priority": "high",
  "requirements": [
    {
      "id": "REQ-17",
      "text": "Debug and fix the food items query in the ProductsPage component to ensure the five seeded food items (Chicken Biryani, Paneer Tikka, Aloo Paratha, Matar Paneer, Matar Paneer Roti) are retrieved from the backend and displayed in the product grid",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-6"
        ],
        "quotes": [
          "No item dukh rha hai"
        ]
      },
      "acceptanceCriteria": [
        "All five seeded food items appear in the product grid on the ProductsPage",
        "Each item displays its name, image, price, categories, and add-to-cart button",
        "Category filtering works correctly to show/hide items based on selected category",
        "No console errors related to data fetching appear in browser developer tools"
      ]
    },
    {
      "id": "REQ-18",
      "text": "Verify the useQueries hook's getAllFoodItems query is correctly configured to fetch data from the backend actor and handle loading/error states appropriately",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-6"
        ]
      },
      "acceptanceCriteria": [
        "The getAllFoodItems query successfully calls the backend actor method",
        "Loading state is displayed while fetching data",
        "Error states are properly handled and displayed to the user",
        "Query invalidation works correctly after cart operations"
      ]
    },
    {
      "id": "REQ-19",
      "text": "Check the InitializeData component execution to confirm that all five food items are successfully seeded on admin login and verify no duplicate prevention logic is blocking legitimate items",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-6"
        ]
      },
      "acceptanceCriteria": [
        "InitializeData component runs on admin login",
        "All five food items are created in the backend without errors",
        "Duplicate check correctly identifies existing items by name",
        "Success/failure states are logged for debugging"
      ]
    },
    {
      "id": "REQ-20",
      "text": "Verify the backend getAllFoodItems function returns the correct data structure that matches the frontend FoodItem type expectations, including all required fields (id, name, description, price, categories array, imageUrl, isAvailable, prepTime)",
      "target": "backend",
      "source": {
        "messageIds": [
          "msg-6"
        ]
      },
      "acceptanceCriteria": [
        "Backend returns an array of food items with all required fields populated",
        "Categories are returned as an array of strings matching the expected category values",
        "Price values are properly formatted as integers",
        "ImageUrl field contains valid placeholder or generated image paths"
      ]
    }
  ],
  "constraints": [
    "Do not modify the backend migration.mo file unless data structure changes are required",
    "Preserve existing authentication flow with Internet Identity",
    "Maintain the existing five seeded food items (Chicken Biryani, Paneer Tikka, Aloo Paratha, Matar Paneer, Matar Paneer Roti)",
    "Do not change the warm, appetizing color scheme already implemented"
  ],
  "nonGoals": [
    "Adding new food items beyond debugging the display issue",
    "Redesigning the product card layout or styling",
    "Implementing new filtering or sorting options",
    "Modifying the order history or cart functionality"
  ],
  "imageRequirements": {
    "required": [],
    "edits": []
  }
}